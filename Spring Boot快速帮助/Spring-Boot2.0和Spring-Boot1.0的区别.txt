1、SpringBoot2.0的改动有哪些？

	session过期时间

		由原来的 server.session.timout 变为了 server.servlet.session.timout  而且后面还要加上时间类型

			例如：
				server.session.timeout=60			默认的时间是秒

				server.servlet.session.timeout=60s		（后面指定时间类型（s，m，h））

	